## 12:58 - 단일 HTML 실행 오류 및 무결성 검증 개선

### 변경 요약

- 생성 후 내용이 달라진 단일 HTML의 CSP 해시가 실제 인라인 JavaScript·CSS와 불일치해 빈 화면이 되던 문제를 정상 재빌드로 복구했다.
- 단일 HTML 빌드가 최종 저장 파일의 CSP 해시를 다시 계산해 검증하도록 하고, 기존 산출물만 검사하는 `verify:single-html` 명령을 추가했다.
- 설정 화면의 버전은 별도 `manifest.json` 요청 대신 빌드 시 내장해 단일 파일 실행에서 상대 경로 요청이 발생하지 않도록 했다.
- `package.json`과 확장 매니페스트 버전이 다르면 빌드를 중단하며, CSP 해시가 포함된 HTML은 Git 줄바꿈 변환 대상에서 제외했다.
- 내부망·외부망·단일 HTML 산출물을 최신 소스와 검증 규칙으로 다시 생성했다.

### 주요 영향 파일

- `.gitattributes`
- `scripts/build-profile-config.mjs`
- `scripts/build-profile-config.d.mts`
- `scripts/build-single-html.mjs`
- `src/build-globals.d.ts`
- `src/pages/SettingsPage.tsx`
- `package.json`
- `dist/assets/app.js`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `node scripts/build-extension.mjs --profile external`
- `node scripts/build-single-html.mjs`
- `npm run verify:single-html`
- 로컬 정적 서버에서 단일 HTML의 대시보드·설정 화면, 버전 `v1.2.0`, 콘솔 오류 없음 확인

### 주의사항

- 단일 HTML은 생성 후 문자열 치환이나 편집을 하면 CSP 무결성 검증에 실패하므로 반드시 소스를 수정한 뒤 다시 빌드해야 한다.
- `file://`에서 기본 UI를 여는 것과 AI API 호출 가능 여부는 별개다. 서버가 `Origin: null` 요청을 허용하지 않으면 AI 기능은 확장 프로그램 또는 허용된 HTTPS 배포 환경에서 사용해야 한다.

## 13:17 - 구버전 백업의 기본 규칙 참조 호환성 복구

### 변경 요약

- 구버전에서 이름 기반 ID로 저장된 프로젝트·일정 종류와 현재 기본 사용자 규칙의 고정 ID가 달라 백업 가져오기가 거부되던 문제를 수정했다.
- 누락 참조가 알려진 기본 항목이면 같은 이름의 기존 항목으로 연결하고, 같은 이름도 없을 때만 기본 항목을 복원한다.
- 데이터베이스 초기화 시에도 동일한 보정을 적용해 이후 내보내는 백업에 끊어진 기본 규칙 참조가 남지 않도록 했다.
- 알려지지 않은 사용자 정의 참조는 자동 보정하지 않고 기존과 같이 검증 오류로 거부한다.

### 주요 영향 파일

- `src/utils/defaultReferenceRepair.ts`
- `src/utils/importBackup.ts`
- `src/db.ts`
- `tests/importBackup.compat.test.ts`
- `dist/assets/app.js`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`

### 주의사항

- 백업 포맷 버전은 5로 유지되며, 기존 버전 4·5 백업의 구조 검증과 용량 제한은 그대로 적용된다.
- 자동 복구 범위는 애플리케이션에 정의된 기본 프로젝트·일정 종류 ID로 제한된다.

## 15:30 - 노트 제목과 볼드 표시 조정

### 변경 요약

- 노트 라이브 편집기의 Heading 1 크기를 기존의 80% 수준으로 낮춰 본문 및 하위 제목과의 시각적 균형을 조정했다.
- 노트의 볼드 텍스트를 링크 색과 구분되는 약간 어두운 빨간색으로 표시하도록 통일했다.

### 주요 영향 파일

- `src/index.css`
- `dist/assets/app.css`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`

### 주의사항

- 색상 변경은 노트의 명시적 볼드 구문에만 적용되며 링크, 헤딩 단계별 색상, 경고 상태 색상에는 영향을 주지 않는다.

## 17:18 - 단일 HTML 파일 실행 호환성 및 볼드 색상 보정

### 변경 요약

- 단일 HTML의 애플리케이션 코드를 ES 모듈 대신 완전히 번들된 고전 스크립트로 생성해 `file://` 직접 실행 시 브라우저별 모듈 보안 제약을 받지 않도록 했다.
- 단일 HTML 검증 단계에서 모듈 스크립트가 다시 포함되면 빌드를 거부하도록 회귀 방지 검사를 추가했다.
- 노트 볼드 텍스트를 기존보다 채도가 높은 어두운 빨간색으로 조정했다.

### 주요 영향 파일

- `scripts/build-single-html.mjs`
- `src/index.css`
- `dist/assets/app.css`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`

### 주의사항

- 단일 HTML은 생성 후 내용을 수정하면 CSP 무결성 검증에 실패할 수 있으므로 반드시 빌드 명령으로 다시 생성해야 한다.
- 단일 HTML의 AI 통신은 파일 실행 여부와 별개로 대상 서버의 CORS 정책에서 `Origin: null`을 허용해야 한다.

## 22:51 - 첫 백업 알림 유예 및 버전 1.2.1

### 변경 요약

- 애플리케이션과 설치용 확장 프로그램 버전을 `1.2.1`로 올렸다.
- 백업 이력이 없는 초기 상태에서는 백업 알림을 즉시 표시하지 않고, 일정·노트·내용이 있는 메모·사용자 정의 분류 데이터가 처음 확인된 시점부터 7일 뒤에 첫 알림을 표시하도록 변경했다.
- 이미 저장된 마지막 내보내기 시각이나 다음 알림 시각은 초기화하지 않고 기존 일정을 유지한다.

### 주요 영향 파일

- `package.json`
- `package-lock.json`
- `public/manifest.json`
- `src/utils/jsonBackup.ts`
- `src/components/WeeklyBackupReminder.tsx`
- `tests/jsonBackupReminder.test.ts`
- `scripts/run-tests.mjs`
- `dist/manifest.json`
- `dist/assets/app.js`
- `dist-web/planai.html`

### 검증

- `npm test`
- `npm run typecheck`
- `npm run lint`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`
- 내부망·외부망 확장 프로그램 매니페스트와 단일 HTML의 버전 `1.2.1` 반영 확인

### 주의사항

- 기본 제공 프로젝트와 일정 종류만 존재하는 상태는 첫 사용자 데이터 생성으로 계산하지 않는다.
- 기존 사용자의 `lastExportedAt` 또는 `nextReminderAt` 값이 있으면 마이그레이션 없이 그대로 사용한다.
- 새 저장 키나 권한은 추가하지 않았으며 백업 데이터 형식도 변경하지 않았다.

## 22:57 - 단일 HTML 기본 AI 모델 분리

### 변경 요약

- 단일 HTML에서 새 설정을 만들 때 사용하는 기본 AI 모델을 `gpt-4o-mini`에서 `gemma4-26b-moe`로 변경했다.
- 기본 모델을 빌드 시점 주입값으로 분리해 내부망·외부망 확장 프로그램은 기존 `gpt-4o-mini` 기본값을 유지하도록 했다.
- 단일 HTML 무결성 검사에 `gemma4-26b-moe` 포함 여부를 추가해 잘못된 기본 모델로 생성된 배포 파일을 거부하도록 했다.

### 주요 영향 파일

- `scripts/build-profile-config.mjs`
- `scripts/build-profile-config.d.mts`
- `scripts/build-single-html.mjs`
- `src/build-globals.d.ts`
- `src/constants.ts`
- `dist/assets/app.js`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`
- 단일 HTML에는 `gemma4-26b-moe`만, 내부망·외부망 확장 번들에는 `gpt-4o-mini`만 포함되는지 확인

### 주의사항

- 이미 브라우저 데이터베이스에 저장된 모델 선택값은 덮어쓰지 않으며, 새 설정 또는 모델값이 없는 설정에만 새 단일 HTML 기본값이 적용된다.
- API Endpoint, API 키 저장 방식, 네트워크 권한과 백업 데이터 형식은 변경하지 않았다.

## 23:00 - 내부망 기본 AI 모델 Gemma 적용

### 변경 요약

- 내부망 확장 프로그램의 새 설정 기본 AI 모델도 단일 HTML과 동일한 `gemma4-26b-moe`로 변경했다.
- 대상별 기본값을 내부망·단일 HTML은 `gemma4-26b-moe`, 외부망 테스트는 `gpt-4o-mini`로 명시적으로 분리했다.
- 내부망 테스트 프로필에서 기본 모델값을 단언하는 회귀 검사를 추가했다.

### 주요 영향 파일

- `scripts/build-profile-config.mjs`
- `scripts/build-profile-config.d.mts`
- `scripts/build-single-html.mjs`
- `tests/llmClient.compat.test.ts`
- `dist/assets/app.js`
- `dist-web/planai.html`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `npm run build:single-html`
- `npm run verify:single-html`
- 내부망 확장 번들과 단일 HTML에는 `gemma4-26b-moe`만, 외부망 테스트 번들에는 `gpt-4o-mini`만 포함되는지 확인

### 주의사항

- 이미 저장된 모델 선택값은 덮어쓰지 않으며, 새 설정 또는 모델값이 없는 설정에만 대상별 기본값이 적용된다.
- API Endpoint, API 키 저장 방식, 네트워크 권한은 변경하지 않았다.
