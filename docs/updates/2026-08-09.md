## 00:19 - 프로젝트 색상 팔레트 개편

### 변경 요약

- 프로젝트와 일정 종류에서 선택할 수 있는 색상 프리셋을 중복도가 낮은 20색 팔레트로 교체했다.
- 20개 프리셋이 10개씩 두 줄로 정렬되도록 선택 그리드를 조정했다.
- 새 프로젝트와 랜덤 색상 선택에는 새 팔레트를 사용하며, 기존 프로젝트와 일정 종류에 저장된 색상값은 변경하지 않는다.

### 주요 영향 파일

- `src/constants.ts`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`

### 주의사항

- 기존 데이터에 대한 색상 마이그레이션은 수행하지 않으므로 기존 항목은 이전 색상을 계속 표시한다.
- 외부 패키지, 권한, 데이터 저장 형식은 변경하지 않는다.

## 17:30 - TODAY 플립 시계 HTML 목업 추가

### 변경 요약

- 대시보드 TODAY 날짜 옆에 배치하는 흰색 배경·검은 숫자 플립 시계 목업을 별도 HTML로 추가했다.
- 현재 시각 표시, 분·시 변경 시 플립 애니메이션, `+1시간` 데모 버튼과 현재 시각 복귀 버튼을 구현했다.
- 플립 중 서로 다른 숫자의 상·하단이 조합되어 다음다음 값처럼 보이던 현상을 없애고, 이전 값과 새 값의 전체 면을 교체하도록 수정했다.
- 전체 카드를 3D 회전시키며 숫자가 왜곡되고 깜빡이던 방식을 제거하고, 변경되는 한 자리 숫자의 상·하단 플랩만 순차적으로 접히도록 다시 구현했다.
- 시·분 사이의 콜론에 검은색 유지, 비선형 감광, 부드러운 복귀로 이어지는 현대적인 호흡형 점멸 애니메이션을 추가했다.
- 플립 시계의 카드와 숫자, 콜론 크기를 축소해 `TODAY`와 날짜 텍스트를 합친 높이에 맞췄다.
- 콜론의 즉시 검은색 복귀는 취소하고, 감광 후 부드럽게 검은색으로 돌아오는 이전 점멸 리듬을 복원했다.
- 좁은 화면 대응과 모션 감소 환경을 위한 접근성 처리를 포함했다.

### 주요 영향 파일

- `docs/mockups/flip-clock.html`

### 검증

- HTML 인라인 스크립트 구문 검사
- 버튼·타이머·반응형·모션 감소 처리 정적 구조 확인

### 주의사항

- 독립 실행형 디자인 목업이며 실제 React 대시보드에는 아직 연결하지 않았다.
- 외부 리소스, 권한, 저장 데이터 형식은 변경하지 않는다.

## 10:41 - 대시보드 주간 보기 이동 기능 추가

### 변경 요약

- 주간 보기 상단에 `이전 주`, `이번 주`, `다음 주` 버튼을 추가했다.
- 이전·다음 주 이동 시 표시 범위를 7일 단위로 변경하고, `이번 주` 버튼으로 현재 주간에 즉시 복귀할 수 있게 했다.
- 현재 주간을 보고 있을 때는 `이번 주` 버튼을 비활성화하고, 좁은 화면에서는 이동 버튼이 별도 줄로 배치되도록 했다.

### 주요 영향 파일

- `src/pages/DashboardPage.tsx`
- `src/pages/DashboardPage.weekNavigation.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 브라우저에서 이전 주·다음 주 이동 시 시작일이 7일씩 변경되고 `이번 주` 복귀가 동작하는지 확인

### 주의사항

- 기존 주 시작 요일 설정을 그대로 따르며 외부 패키지, 권한, 저장 데이터 형식은 변경하지 않는다.

## 10:48 - 일정 시간 선택 UI 개선

### 변경 요약

- 일정 추가·수정 폼의 브라우저 기본 시간 입력을 시·분 선택 UI로 교체했다.
- 12시간제에서는 오전·오후, 시, 분을 분리하고 24시간제에서는 시와 분을 분리해 표시한다.
- 분 선택지는 5분 간격으로 제한하되 기존 일정이 다른 분 값을 사용하는 경우 원래 값을 별도 선택지로 보존한다.
- 종료 시간에는 선택값을 한 번에 제거할 수 있는 `비우기` 버튼을 추가했다.

### 주요 영향 파일

- `src/components/TaskForm.tsx`
- `src/components/TimeSelect.tsx`
- `src/components/TaskForm.timeSelect.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 24시간제와 12시간제 표시, 오전·오후 전환, 5분 간격 분 선택 및 미리보기 반영 확인

### 보안·호환성 관련 주의사항

- 외부 패키지·네트워크 요청·권한을 추가하지 않았으며 기존 일정의 비정형 분 값을 임의로 반올림하거나 유실하지 않는다.

## 10:51 - 종료 일시 일괄 지우기 추가

### 변경 요약

- 고급 옵션의 종료 날짜 입력 옆에 `지우기` 버튼을 추가했다.
- 버튼을 누르면 종료 날짜와 종료 시간을 한 번의 상태 변경으로 함께 비우도록 처리했다.
- 지울 종료 정보가 없을 때는 버튼을 비활성화하고, 종료 시간의 개별 비우기 버튼은 제거해 불완전한 입력 상태를 방지했다.

### 주요 영향 파일

- `src/components/TaskForm.tsx`
- `src/components/TaskForm.timeSelect.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 종료 날짜와 `14:35`를 입력한 뒤 `지우기`를 눌러 날짜·시·분이 모두 초기화되는지 확인

### 보안·호환성 관련 주의사항

- 외부 패키지·권한·저장 형식은 변경하지 않았으며 종료 일시만 명시적인 사용자 동작으로 제거한다.

## 10:55 - 날짜·시간 입력 박스 높이 통일

### 변경 요약

- 시작·종료 시간 선택 상단의 `시`, `분` 안내 문구를 제거했다.
- 12시간제의 `구분` 안내 문구도 제거하고 오전·오후 선택값만 표시하도록 정리했다.
- 시간 선택 박스와 종료 날짜 컨트롤의 높이·내부 여백을 일반 날짜 입력과 동일한 크기로 조정했다.
- 화면낭독기용 시·분·오전/오후 접근성 이름은 유지했다.

### 주요 영향 파일

- `src/components/TimeSelect.tsx`
- `src/components/TaskForm.timeSelect.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 시작 날짜·시간 박스 높이와 시작·종료 시간의 안내 문구 제거 상태를 확인

### 보안·호환성 관련 주의사항

- 시간값 처리와 저장 형식은 변경하지 않았으며 외부 패키지·권한을 추가하지 않았다.

## 11:09 - 일정 날짜·시간 컨트롤 배경색 통일

### 변경 요약

- 시작·종료 시간의 시·분 선택 상자 내부 배경을 흰색으로 변경했다.
- 종료 날짜 입력과 종료 일시 `지우기` 버튼의 기본 배경도 흰색으로 통일했다.
- 포커스와 마우스 오버 상태의 시각적 구분은 유지했다.

### 주요 영향 파일

- `src/components/TaskForm.timeSelect.css`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 시작 시간과 종료 날짜·시간의 흰색 배경 및 포커스 상태 확인

### 보안·호환성 관련 주의사항

- 표시 스타일만 변경했으며 시간값 처리, 저장 형식, 외부 권한은 변경하지 않았다.

## 12:19 - Playwright 및 Chromium 설치

### 변경 요약

- 공식 Playwright 테스트 패키지 `@playwright/test`를 개발 의존성으로 추가했다.
- 프로젝트의 버전 고정 정책에 맞춰 `1.62.1`로 정확히 고정했다.
- 로컬 E2E 테스트용 Chromium 브라우저 바이너리를 설치했다.
- 자동 생성 설정과 예제 테스트 파일은 추가하지 않았다.

### 주요 영향 파일

- `package.json`
- `package-lock.json`

### 검증

- `npx playwright --version`
- `npm ls @playwright/test playwright playwright-core`
- Chromium 헤드리스 실행 및 테스트 페이지 제목 확인
- `npm audit`
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `git diff --check`

### 보안·호환성 관련 주의사항

- Playwright 공식 패키지만 개발 의존성으로 사용하며 npm 감사에서 알려진 취약점 0건을 확인했다.
- Chromium만 설치해 불필요한 브라우저 바이너리와 저장 공간 사용을 제한했다.

## 13:18 - 생성 메뉴·모바일 노트 탐색·모달 버튼 정리

### 변경 요약

- 상단의 노트 추가와 AI 일정 추가 버튼을 `만들기` 메뉴로 통합하고, 일반 일정 추가도 기존 일정 모달로 바로 연결했다.
- 모바일 노트 화면에서 탐색기를 기본적으로 접어 본문을 먼저 표시하고, 필요할 때 `노트 탐색` 버튼으로 펼칠 수 있게 했다.
- 노트 선택·편집 또는 새 노트 생성 후에는 모바일 탐색기를 자동으로 닫아 본문에 바로 집중할 수 있게 했다.
- 노트 추가 모달은 취소·확인 버튼을 우측 하단에 배치하고, 질문 모달의 실행 버튼도 입력창 아래 우측으로 정렬했다.
- 기존 문구는 변경하지 않았다.

### 주요 영향 파일

- `src/components/AppShell.tsx`
- `src/components/AskDataModal.tsx`
- `src/components/NoteQuickAddModal.tsx`
- `src/pages/DashboardPage.tsx`
- `src/pages/NotesPage.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 `만들기` 메뉴의 일정·AI 일정·노트 항목 및 일반 일정 모달 연결 확인
- 390×844 모바일 화면에서 노트 본문 우선 표시, 탐색기 펼침·닫힘, 모바일 메뉴 5개 항목 확인
- 390×844 모바일 화면에서 질문 버튼의 입력창 아래 우측 배치와 노트 모달의 취소·확인 순서·우측 정렬 확인

### 보안·호환성 관련 주의사항

- 새 외부 패키지, 권한, 네트워크 요청은 추가하지 않았고 기존 일정·노트 생성 로직만 재사용했다.
- 메뉴의 외부 클릭·Escape 닫힘과 모달 포커스 관리를 유지했으며, 빌드별 엔드포인트 분리는 변경하지 않았다.

## 13:37 - 상단 일정 추가 동선 단순화

### 변경 요약

- 상단 `만들기` 메뉴를 제거하고 `AI 일정 추가`와 `수동 추가` 버튼을 바로 표시하도록 변경했다.
- `AI 일정 추가`는 기존 AI 일정 모달을, `수동 추가`는 기존 일정 입력 모달을 연다.
- 대시보드의 AI 브리핑·보기 전환 옆에 있던 중복 `일정 추가` 버튼을 제거했다.
- 선택 날짜와 빈 화면 등 문맥에 필요한 일정 추가 버튼은 유지했다.

### 주요 영향 파일

- `src/components/AppShell.tsx`
- `src/pages/DashboardPage.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 두 상단 버튼의 표시와 AI·수동 일정 모달 연결 확인
- 320px·390px 화면에서 두 버튼 표시, 화면 밖 넘침 없음, 가로 스크롤 미발생 확인
- 대시보드 보기 전환 영역의 중복 일정 추가 버튼 제거 확인

### 보안·호환성 관련 주의사항

- 새 외부 패키지, 권한, 저장 데이터 형식, 네트워크 요청은 추가하지 않았다.
- 기존 AI 일정 및 수동 일정 모달을 그대로 재사용하므로 일정 생성·검증 로직은 변경하지 않았다.

## 13:42 - 상단 수동 추가를 노트 추가로 교체

### 변경 요약

- 상단 `수동 추가` 버튼을 제거하고 `노트 추가` 버튼으로 교체했다.
- `노트 추가`는 현재 화면의 URL이나 경로를 바꾸지 않고 바로 노트 입력 모달을 연다.
- 수동 추가에 사용하던 `dashboard?create=1` 이동과 대시보드의 해당 쿼리 처리 로직을 제거했다.
- 상단 `AI 일정 추가` 역시 현재 화면에서 기존 AI 일정 모달을 직접 여는 동작을 유지했다.

### 주요 영향 파일

- `src/components/AppShell.tsx`
- `src/pages/DashboardPage.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 상단 AI 일정 추가·노트 추가 버튼 표시 및 수동 추가 버튼 제거 확인
- 두 버튼을 눌렀을 때 URL이 유지되고 각 모달이 직접 열리는지 확인
- 320px·390px 화면에서 두 버튼의 표시와 화면 넘침 여부 확인

### 보안·호환성 관련 주의사항

- 새 외부 패키지, 권한, 저장 데이터 형식, 네트워크 요청은 추가하지 않았다.
- 노트 저장 시 기존 입력 검증과 프로젝트 지정 로직을 재사용하며, URL 쿼리에 의존하던 수동 추가 분기는 제거했다.

## 13:43 - 상단 추가 버튼 순서 변경

### 변경 요약

- 상단 버튼 순서를 `노트 추가` 다음에 `AI 일정 추가`가 오도록 변경했다.
- AI 일정 추가의 기본 강조 스타일과 각 버튼의 동작은 유지했다.

### 주요 영향 파일

- `src/components/AppShell.tsx`
- `dist/assets/app.js`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`

### 보안·호환성 관련 주의사항

- 버튼의 표시 순서만 변경했으며 권한, 데이터 처리, 네트워크 요청에는 영향이 없다.

## 15:28 - 노트 탭 탐색·목록·상세 구조 개편

### 변경 요약

- 노트 탭을 `탐색 + 노트 목록`으로 시작하고, 노트 선택 후에는 접힌 탐색 레일·목록·선택 노트 상세 구조로 전환하도록 개편했다.
- 선택 후 접힌 탐색은 왼쪽 가장자리의 마우스 접근 또는 키보드 버튼으로 임시 노출되며, 모바일에서는 드로어와 목록→상세 전환으로 동작한다.
- 모든 노트를 본문에 반복하던 이어보기 화면을 제거하고 한 번 클릭한 노트 하나만 읽기·편집하도록 통일했다.
- 다중 선택 체크박스는 선택 모드에서만 표시하고, 노트 카드에 본문 미리보기·상태·수정 시각을 추가했다.
- 편집기의 자동 저장 상태를 일반 텍스트로 표시하고 AI는 상단 주요 작업으로, 변경 이력과 삭제는 더보기 메뉴로 정리했다.
- 전역 및 노트 탭의 노트 추가 동작을 생성 즉시 편집하는 동일한 흐름으로 통합하고 `Ctrl+N`, `Ctrl+K` 단축키를 지원한다.
- 프로젝트의 세부 항목 추가는 프로젝트별 더보기 버튼으로 축소하고, 노트 탭의 주간 백업 알림은 작업을 가리지 않는 소형 형태로 표시한다.

### 주요 영향 파일

- `src/pages/NotesPage.tsx`
- `src/components/NoteCard.tsx`
- `src/components/NoteEditor.tsx`
- `src/components/ProjectNoteTree.tsx`
- `src/components/AppShell.tsx`
- `src/components/WeeklyBackupReminder.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 탐색 자동 접힘, 왼쪽 가장자리 재노출, 단일 노트 상세, 다중 선택, 편집기 더보기, 생성 즉시 편집 확인

### 보안·호환성 관련 주의사항

- 새 외부 패키지, 권한, 네트워크 요청, 저장 데이터 형식은 추가하거나 변경하지 않았다.
- 숨겨진 탐색 패널은 `inert` 처리해 접힌 상태에서 키보드 포커스가 진입하지 않으며 기존 자동 저장·삭제 확인·AI 제안 검토 흐름을 유지한다.

## 15:56 - 노트 선택 화면 시각 구조 재정비

### 변경 요약

- 선택 후 남던 긴 흰색 탐색 레일을 제거하고 왼쪽의 작은 감지선과 목록 상단 탐색 버튼으로 대체했다.
- 임시 탐색 패널의 너비·그림자·모서리를 축소해 목록을 과도하게 가리지 않도록 하고 명시적인 닫기 버튼을 유지했다.
- 선택 노트의 프로젝트 색 외곽선, 거대한 최소 높이, 중첩된 읽기 카드 배경을 제거해 문서형 상세 화면으로 변경했다.
- 노트 목록 카드를 차분한 행 형태로 정리하고 선택 상태만 옅은 파란색으로 강조했다.
- 읽기 화면의 중복 편집 버튼과 메타데이터 옆 장식 아이콘을 제거했다.
- 노트 선택 시 스크롤 위치를 상단으로 안정화하고 목록·탐색 고정 위치가 상단 내비게이션 아래에 유지되도록 수정했다.

### 주요 영향 파일

- `src/pages/NotesPage.tsx`
- `src/components/NoteEditor.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 선택 화면, 읽기·편집 전환, 탐색 호버·버튼 열기·닫기, 상단 정렬을 확인

### 보안·호환성 관련 주의사항

- 표시 구조와 화면 상태 전환만 변경했으며 외부 패키지, 권한, 네트워크 요청, 저장 데이터 형식에는 영향이 없다.
- 탐색 패널의 `inert` 접근성 처리와 기존 자동 저장·삭제 확인 절차를 유지한다.

## 16:01 - 노트 AI 메뉴 위치 정렬

### 변경 요약

- AI 편집 메뉴를 노트 화면 내부가 아닌 화면 기준 레이어에 표시해 편집 영역의 위치만큼 오른쪽으로 밀리던 문제를 수정했다.
- AI와 더보기 버튼에서 연 메뉴는 버튼의 오른쪽 끝에 맞춰 6px 아래에 표시하고, 긴 메뉴는 남은 화면 높이 안에서 스크롤하도록 조정했다.
- 우클릭으로 여는 기존 작업 메뉴는 포인터 위치를 유지하면서 화면 가장자리를 벗어나지 않도록 했다.

### 주요 영향 파일

- `src/components/ContextMenu.tsx`
- `src/pages/NotesPage.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 AI 버튼과 메뉴 오른쪽 끝 일치, 6px 세로 간격, 긴 메뉴 내부 스크롤을 확인

### 보안·호환성 관련 주의사항

- 메뉴 렌더링 좌표와 표시 레이어만 변경했으며 권한, 네트워크 요청, 저장 데이터, AI 요청 내용에는 영향이 없다.
- 외부 패키지를 추가하지 않았고 기존 키보드 탐색·바깥 클릭 닫기 동작을 유지한다.

## 17:24 - 노트 탐색 복귀 동작 단순화

### 변경 요약

- 노트 선택 화면의 목록 상단 햄버거 버튼을 누르면 선택 상세를 닫고 필터를 초기화해 `탐색 + 전체 노트` 화면으로 돌아가도록 변경했다.
- 선택 화면 왼쪽의 감지 막대와 마우스 접근 시 나타나던 슬라이드 탐색 패널을 제거했다.
- 선택 화면을 `노트 목록 + 선택 노트` 2열 구조로 단순화하고 모바일의 목록 복귀도 전체 노트 기준으로 통일했다.

### 주요 영향 파일

- `src/pages/NotesPage.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 선택 화면의 감지 막대·슬라이드 탐색 제거와 햄버거 버튼의 `탐색 + 전체 노트` 복귀를 확인

### 보안·호환성 관련 주의사항

- 화면 상태와 레이아웃만 변경했으며 권한, 네트워크 요청, 저장 데이터 형식에는 영향이 없다.
- 화면 복귀 시 기존 자동 저장 절차를 유지해 편집 중인 노트 내용이 유실되지 않도록 했다.

## 17:29 - 노트 읽기 화면 문서 스타일 적용

### 변경 요약

- 읽기 모드의 제목·분류·본문 전체를 흰색 문서 카드로 묶어 바깥 화면과 명확히 구분되도록 변경했다.
- 프로젝트 색상을 절제된 상단 테두리로 사용하고 얇은 외곽선, 둥근 모서리, 약한 그림자와 본문 구분선을 적용했다.
- 본문 여백과 줄 간격을 읽기 용도에 맞게 조정하고 모바일에서는 화면 폭에 맞는 여백과 최소 높이를 적용했다.
- 편집 모드의 기존 투명 컨테이너와 입력 동작은 유지했다.

### 주요 영향 파일

- `src/components/NoteEditor.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 읽기 모드 문서 카드와 편집 모드 기존 스타일 유지를 확인

### 보안·호환성 관련 주의사항

- 표시 스타일과 모드 식별 클래스만 변경했으며 권한, 네트워크 요청, 저장 데이터 형식에는 영향이 없다.
- 외부 패키지나 외부 리소스를 추가하지 않았다.

## 17:36 - 노트 고정 유지 및 관련 정보 영역 복원

### 변경 요약

- 선택 중인 노트의 고정·상태 변경 시 편집 초안과 저장 데이터를 동시에 갱신해 이전 초안의 자동 저장이 변경값을 되돌리던 문제를 수정했다.
- 저장 실패 시 화면의 메타데이터를 이전 값으로 되돌리고 오류를 안내하도록 처리했다.
- 관련 일정과 관련 노트를 두 개의 독립된 카드로 항상 표시하고 결과가 없을 때는 간결한 빈 상태를 표시하도록 복원했다.
- 연결된 일정과 자동 추천 일정은 관련 일정 카드 안에서 함께 표시하며 모바일에서는 한 열로 배치한다.

### 주요 영향 파일

- `src/pages/NotesPage.tsx`
- `src/components/NoteConnections.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 데스크톱 브라우저에서 고정 후 자동 저장 대기·새로고침 뒤 유지, 고정 해제 유지, 관련 일정·관련 노트 빈 상태 표시를 확인

### 보안·호환성 관련 주의사항

- 기존 로컬 노트 저장 API와 데이터 형식을 유지했으며 권한과 네트워크 요청은 변경하지 않았다.
- 메타데이터 변경 시 선택 초안을 동기화해 자동 저장 경쟁 조건으로 인한 데이터 덮어쓰기를 방지한다.

## 17:44 - 통합 노트 원본 보관 및 연결 표시

### 변경 요약

- AI 노트 통합이 성공하면 통합 노트를 생성하고 선택한 원본 노트들을 보관 상태로 전환하도록 변경했다.
- 통합 노트에 원본 노트 ID를 저장하고 관련 노트 영역에서 `[원본]` 배지와 함께 표시하도록 했다.
- 원본 연결은 자동 관련 노트 추천 설정이 꺼져 있어도 항상 표시하며 자동 추천 결과와 중복되지 않도록 처리했다.
- 통합 노트 생성, 원본 존재 재확인, 원본 보관을 하나의 로컬 DB 트랜잭션으로 묶었다.
- 원본 노트를 영구 삭제하면 다른 통합 노트의 원본 연결에서도 해당 ID를 정리하도록 했다.

### 주요 영향 파일

- `src/models.ts`
- `src/context/AppDataContext.tsx`
- `src/pages/NotesPage.tsx`
- `src/components/NoteConnections.tsx`
- `src/utils/importBackup.ts`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 기존 노트 선택과 관련 일정·관련 노트 영역 표시 회귀 확인

### 보안·호환성 관련 주의사항

- 원본 연결은 최대 200개로 제한하고 식별자 형식, 자기 참조, 백업 복원 시 참조 무결성을 검증한다.
- 통합 생성과 원본 보관은 원자적으로 처리해 중간 실패 시 일부 원본만 보관되는 상태를 방지한다.
- 기존 백업의 원본 연결 필드가 없어도 빈 배열로 호환 처리하며 외부 패키지와 추가 네트워크 요청은 없다.

## 17:55 - 노트 마크다운 편집 기능 대폭 강화

### 변경 요약

- GitHub Flavored Markdown 형식의 표, 열 정렬, 하이라이트, 자동 링크, 이미지 링크 안내, 언어 표기가 있는 코드 블록 렌더링을 추가했다.
- 편집 모드에 제목, 굵게, 기울임, 취소선, 코드, 링크, 목록, 체크리스트, 인용, 코드 블록, 표, 구분선 도구막대를 추가했다.
- 글머리·번호·체크리스트·인용 끝에서 Enter를 누르면 다음 항목을 자동 생성하고 빈 항목에서 다시 Enter를 누르면 목록을 종료하도록 했다.
- Tab과 Shift+Tab으로 목록 들여쓰기·내어쓰기를 지원하고 빈 목록 접두어에서 Backspace로 일반 문단으로 돌아가도록 했다.
- 읽기 화면의 렌더링된 마크다운 블록을 클릭하면 해당 위치에서 원문을 바로 편집하고 Ctrl+Enter 또는 Esc로 완료할 수 있도록 했다.
- 굵게, 기울임, 링크, 취소선, 코드, 제목, 목록, 체크리스트, 인용, 표 단축키를 추가하고 도움말에 정리했다.

### 주요 영향 파일

- `src/components/MarkdownRenderer.tsx`
- `src/components/MarkdownComposer.tsx`
- `src/components/InlineMarkdownEditor.tsx`
- `src/components/NoteEditor.tsx`
- `src/components/HelpModal.tsx`
- `src/utils/markdownEditing.ts`
- `src/index.css`
- `tests/markdownEditing.test.ts`
- `scripts/run-tests.mjs`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 목록 Enter 연속·종료, Ctrl+B, 표 렌더링, 읽기 블록 직접 편집과 Esc 완료를 확인

### 보안·호환성 관련 주의사항

- 외부 패키지를 추가하지 않았고 기존 마크다운 원문과 자동 저장 데이터 형식을 유지한다.
- 링크는 기존 URL 프로토콜·인증정보 검증을 유지하며 이미지 문법은 외부 이미지를 자동 요청하지 않고 안전한 링크로 표시한다.
- 표와 편집 블록은 청크 단위로 렌더링하며 무한 반복, 백그라운드 요청, 추가 권한을 사용하지 않는다.

## 18:12 - 노트 전체 문서 라이브 편집으로 전환

### 변경 요약

- 앞선 블록별 직접 편집 방식을 제거하고, 렌더링된 노트 전체를 하나의 연속된 편집 영역에서 수정하도록 변경했다.
- `읽기` 모드를 `라이브`로, 기존 원문 편집 모드를 `원문`으로 명확히 구분하고 라이브 화면에 동일한 서식 도구막대를 제공했다.
- 제목, 문단, 목록, 체크리스트, 표, 인용, 코드 블록을 문서 흐름 안에서 편집하고 Markdown 원문으로 다시 저장하도록 DOM 직렬화를 추가했다.
- 목록에서 Enter로 다음 항목을 만들고 빈 항목에서 Enter를 다시 누르면 일반 문단으로 빠져나오도록 했으며, Tab과 Shift+Tab 들여쓰기를 유지했다.
- 표·코드 블록·체크리스트·구분선을 삽입할 때 현재 문단을 안전하게 나누어 블록 요소가 문단 내부에 잘못 중첩되지 않도록 했다.
- 라이브 화면에서 체크박스 변경, 전체 문서 교체, 굵게 단축키와 표 삽입 결과가 원문에 즉시 반영되도록 했다.

### 주요 영향 파일

- `src/components/LiveMarkdownEditor.tsx`
- `src/components/MarkdownRenderer.tsx`
- `src/components/NoteEditor.tsx`
- `src/components/HelpModal.tsx`
- `src/pages/NotesPage.tsx`
- `src/utils/markdownEditing.ts`
- `src/index.css`
- `tests/markdownEditing.test.ts`
- `dist/assets/app.js`
- `dist/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- `git diff --check`
- 브라우저에서 단일 라이브 편집 영역 표시, 전체 문서 교체, `Ctrl+B`, 체크리스트 저장, 표 삽입과 Markdown 원문 복원을 확인

### 보안·호환성 관련 주의사항

- 외부 편집기 패키지를 추가하지 않고 브라우저 기본 편집 API와 기존 React 렌더러만 사용했다.
- 붙여넣기와 드롭은 HTML을 받지 않고 일반 텍스트만 최대 50만 자까지 처리하며, 링크는 `http`, `https`, `mailto`만 허용하고 인증정보가 포함된 URL을 거부한다.
- 기존 Markdown 저장 형식과 원문 편집 모드를 유지하며 네트워크 요청, 확장 권한, API 키 처리 방식은 변경하지 않았다.

## 21:26 - 라이브 편집 Markdown 제목 자동 전환

### 변경 요약

- 라이브 편집의 일반 문단 시작에서 `# `부터 `###### `까지 입력하면 즉시 해당 단계의 렌더링된 제목으로 전환하도록 했다.
- 제목 접두어를 제거한 뒤에도 입력 커서를 새 제목 안에 유지하고, 이후 입력한 내용이 `# 제목` 형식의 Markdown 원문으로 저장되도록 했다.
- 라이브 편집과 원문 편집 도구막대에 H3 버튼을 추가하고 `Ctrl+Alt+3` 단축키를 연결했다.
- 도움말에 제목 접두어 입력과 H1·H2·H3 단축키를 추가했다.

### 주요 영향 파일

- `src/components/LiveMarkdownEditor.tsx`
- `src/components/MarkdownComposer.tsx`
- `src/components/HelpModal.tsx`
- `tests/markdownEditing.test.ts`
- `dist/assets/app.js`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 브라우저에서 `# 제목`, `## 제목`, `### 제목`의 H1·H2·H3 즉시 전환과 원문 저장을 확인
- 브라우저에서 H3 버튼 표시와 일반 문단의 H3 전환을 확인

### 보안·호환성 관련 주의사항

- 외부 라이브러리, 네트워크 요청, 확장 권한을 추가하지 않았다.
- 일반 문단의 줄 시작에서 1~6개의 `#` 뒤에 공백이 입력된 경우에만 제목으로 전환해 기존 본문의 `#` 문자를 보존한다.
- 기존 Markdown 저장 형식과 H1·H2 단축키 동작을 유지한다.

## 22:07 - 라이브 편집 활성 문법과 슬래시 명령 추가

### 변경 요약

- 라이브 편집에서 커서가 위치한 블록에만 제목, 인용, 목록, 강조, 링크, 코드 등 Markdown 문법 기호를 표시하도록 했다.
- 줄 시작에서 `/`를 입력하면 제목, 목록, 체크리스트, 인용, 코드 블록, 표, 구분선을 검색·실행할 수 있는 명령 메뉴를 표시하도록 했다.
- 슬래시 명령은 한글 검색과 마우스 선택뿐 아니라 위·아래 방향키, Enter·Tab 실행, Esc 닫기를 지원하고 도움말에 사용법을 추가했다.
- 내부 노트 링크, 표 전용 편집 도구, 콜아웃·각주 기능은 이번 변경 범위에서 제외했다.

### 주요 영향 파일

- `src/components/LiveMarkdownEditor.tsx`
- `src/components/HelpModal.tsx`
- `src/utils/markdownEditing.ts`
- `src/index.css`
- `tests/markdownEditing.test.ts`
- `dist/assets/app.js`
- `dist/assets/app.css`
- `dist-external/assets/app.js`
- `dist-external/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 브라우저에서 `/제목` 한글 필터링, 방향키·Enter로 H2 실행, 메뉴 자동 닫힘을 확인
- 브라우저에서 활성 H2 블록에만 `##` 문법 기호가 표시되는 것을 확인

### 보안·호환성 관련 주의사항

- 외부 패키지, 네트워크 요청, 확장 권한을 추가하지 않고 기존 편집 명령과 Markdown 저장 형식을 재사용했다.
- 슬래시 검색어는 24자, 표시 명령은 최대 10개로 제한하고 IME 조합 중 명령 실행을 차단하며 예약 타이머는 해제한다.
- 활성 문법 기호는 CSS 가상 요소로만 표시되어 저장 데이터에 섞이지 않으며 기존 원문·라이브 전환과 자동 저장 방식은 유지한다.

## 23:57 - 노트 편집 도구 간소화 및 하이라이트 전환 개선

### 변경 요약

- 라이브·원문 편집 도구막대에서 이탤릭, 링크 삽입, 인라인 코드 기능과 해당 편집 단축키를 제거했다.
- 기존 노트에 저장된 이탤릭, 링크, 인라인 코드 Markdown은 호환성을 위해 계속 렌더링하도록 유지했다.
- 하이라이트는 선택한 텍스트가 있을 때만 적용하고, 하이라이트 내부에 커서를 둔 뒤 버튼을 다시 누르면 해당 하이라이트가 해제되도록 변경했다.
- 여러 인라인 요소나 문단을 함께 선택해도 블록 구조를 깨지 않고 선택된 텍스트 노드만 하이라이트하도록 처리했다.

### 주요 영향 파일

- `src/components/LiveMarkdownEditor.tsx`
- `src/components/MarkdownComposer.tsx`
- `src/components/HelpModal.tsx`
- `src/index.css`
- `dist/assets/app.js`
- `dist/assets/app.css`
- `dist-external/assets/app.js`
- `dist-external/assets/app.css`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 브라우저에서 라이브·원문 편집 도구막대의 이탤릭·링크·인라인 코드 버튼 제거를 확인
- 브라우저에서 선택 없는 하이라이트 차단, 선택 영역 적용, 커서 내부 재실행 해제를 확인

### 보안·호환성 관련 주의사항

- 하이라이트 처리는 현재 편집 영역에 속한 텍스트 노드만 대상으로 하며 선택 없이 임의의 자리표시자나 마크업을 삽입하지 않는다.
- 외부 패키지, 네트워크 요청, 권한을 추가하지 않았고 기존 Markdown 저장 형식과 읽기 렌더링 호환성을 유지한다.
- 제거된 편집 단축키는 라이브 편집 영역에서 기본 브라우저 서식이 우회 실행되지 않도록 차단한다.
