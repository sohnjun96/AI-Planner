## 21:35 - 상단 설정 아이콘 가시성 개선

### 변경 요약

- 작은 문자형 톱니바퀴를 24px SVG 아이콘으로 교체하고 44px 버튼에 배경과 테두리를 적용했다.
- 마우스 호버, 설정 페이지의 활성 표시, 키보드 포커스를 명확히 표시한다.
- 확장 프로그램 및 단일 HTML·ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/components/AppShell.tsx`, `src/index.css`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`
- `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`: 타입 검사와 빌드, 단일 HTML 무결성 검사 통과.
- `npm run lint` 통과.
- 독립 Chromium의 1440px·390px·320px 화면에서 버튼·아이콘 크기와 가로 넘침을 확인하고 PC·320px 캡처를 육안 검토했다.
- 키보드 포커스와 Enter 키의 설정 이동, 활성 버튼의 호버 대비 확인. CSS 전환 도중의 중간 색상으로 발생한 최초 검사 실패는 전환 완료를 기다리는 검사로 재확인하여 통과했다.
- 브라우저 런타임 오류 없음. 확장 ZIP 10개 파일 및 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- 화면 표시만 변경하며 데이터 스키마, 네트워크 권한, 앱 버전 변경 없음.
- SVG를 앱에 내장하여 추가 외부 요청 없이 표시한다. 설치된 확장은 갱신된 `dist`로 다시 로드해야 적용된다.

## 21:36 - 대시보드 AI 버튼 위치 및 브리핑 크기 조정

### 변경 요약

- AI 일정 추가 버튼을 상단 날짜 영역에서 일정 보드의 `dashboard-card-header`로 옮기고 오른쪽 정렬했다.
- AI 브리핑의 아래 설명을 제거하고 제목을 13px에서 16px로 확대했다. PC 기준 버튼 폭은 약 179px에서 123px로 줄었다.
- 확장 프로그램 및 단일 HTML·ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/pages/DashboardPage.tsx`, `src/components/DailyBriefing.tsx`, `src/index.css`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`, `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`, `npm run lint`, `git diff --check` 통과.
- 독립 Chromium의 1440px·390px·320px에서 목록·주간·월간 보기별 버튼 오른쪽 정렬, 제목과의 겹침 및 가로 넘침 검사 통과.
- PC·320px 화면의 날짜 영역과 일정 보드 헤더 캡처 육안 확인. 브리핑 글자 크기·버튼 폭·설명 제거 확인.
- AI 일정 추가와 브리핑 창 열기·닫기 확인, 브라우저 런타임 오류 없음. 검증 중 외부 API 요청은 차단했다.
- 확장 ZIP 10개 파일 및 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- 버튼 배치와 표시만 변경하며 AI 처리, 저장 데이터, 네트워크 권한, 앱 버전 변경 없음.
- 설치된 확장은 갱신된 `dist`로 다시 로드해야 적용된다.

## 21:38 - AI 브리핑 연결 실패 문구 정리

### 변경 요약

- AI 브리핑에서 `Failed to fetch` 등 브라우저의 네트워크 오류 문구를 `AI 연결 실패`로 표시한다.
- HTTP 상태 코드 등 구체적인 오류 안내와 재시도 동작은 유지한다.
- 확장 프로그램 및 단일 HTML·ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/components/DailyBriefing.tsx`
- `dist/assets/app.js`, `dist.zip`, `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`, `npm run lint`, `git diff --check` 통과.
- 독립 Chromium에서 요청 차단으로 연결 실패를 재현하여 `AI 연결 실패` 표시 및 원문 미노출 확인. 캡처 육안 확인.
- 로컬 모의 응답으로 재시도 성공 시 오류 제거와 브리핑 표시, HTTP 503 시 기존 상세 안내 유지 확인. 실제 외부 요청 없이 검증했으며 런타임 오류 없음.
- 확장 ZIP 10개 파일 및 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- AI 브리핑의 오류 표시만 변경하며 저장 데이터, 네트워크 권한, 앱 버전 변경 없음.
- 설치된 확장은 갱신된 `dist`로 다시 로드해야 적용된다.

## 21:40 - 나의 루틴 카드 정보 구성과 프로젝트 색상 개선

### 변경 요약

- 카드 왼쪽 색상선, 프로젝트 배지와 날짜 영역에 기존 프로젝트 색상을 적용했다. 프로젝트명도 함께 표시하여 색상 외에도 구분할 수 있다.
- 이름·반복 주기·예정일을 중심으로 정보를 배치하고, 중복 안내 문장과 예정 상태 배지를 제거했다. 같은 날 알림은 ‘당일 알림’으로 축약한다.
- 메모는 접어서 표시하고 처리 이력의 펼치기 동작을 유지했다. 주요 실행 버튼과 수정·일시 중지·삭제 도구를 구분했다.
- 공유 컴포넌트를 사용하는 대시보드 루틴 카드에도 적용하고, 320px 화면의 루틴 추가 버튼 줄바꿈을 방지했다.
- 확장 프로그램, 단일 HTML 및 두 ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/components/RoutineList.tsx`, `src/pages/RoutinesPage.css`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`
- `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`: 타입 검사, 빌드 및 단일 HTML 무결성 검사 통과.
- `npm run lint`, `npm run test:routines`, `git diff --check` 통과.
- 독립 Chromium에서 프로젝트 4색, 확인 필요·예정·중지 상태, 메모 키보드 펼치기, 확인 완료와 처리 이력, 프로젝트가 없는 경우, 긴 제목·프로젝트명·메모, 다음 연도의 날짜 표시를 확인했다.
- PC 1440px 및 모바일 390px·320px 화면을 캡처하고 PC·320px 화면을 육안 검토했다. 카드와 대시보드의 가로 넘침 없음. 320px 추가 버튼의 줄바꿈 수정도 재확인했다.
- 브라우저 런타임 오류 없음. 확장 ZIP 10개 파일과 단일 HTML ZIP 1개 파일의 원본 해시 일치를 확인했다.

### 주의사항

- 표시와 스타일만 변경하며 데이터 스키마, 루틴 처리 규칙, 네트워크 권한과 앱 버전은 변경하지 않는다.
- 기존 프로젝트 색상과 앱 내장 SVG를 사용하며 추가 외부 요청이 없다. 설치된 확장은 갱신된 `dist`를 다시 로드해야 적용된다.

## 21:41 - AI 브리핑 버튼을 날짜 요약 옆으로 이동

### 변경 요약

- AI 브리핑 버튼을 `dashboard-summary-trigger` 바로 오른쪽에 배치하고 16px 간격으로 정렬했다.
- 날짜 요약과 브리핑을 같은 영역으로 묶고, 좁은 화면에서는 자연스럽게 줄바꿈한다. 보기 방식 선택은 기존 오른쪽 영역을 유지한다.
- 기존 브리핑 버튼의 52px 높이를 유지하고 확장·단일 HTML·ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/pages/DashboardPage.tsx`, `src/index.css`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`, `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`, `npm run lint`, `git diff --check` 통과.
- 독립 Chromium의 1440px·1024px·768px에서 날짜 요약과 브리핑의 가로 간격·중앙 정렬 확인. 390px·320px를 포함해 가로 넘침 및 요약 펼치기·접기 검사 통과.
- PC·320px 캡처 육안 확인. 날짜 요약에서 Tab 키로 브리핑 이동, Enter 키로 창 열기·닫기와 연결 실패 문구 확인. 외부 API 요청은 차단했으며 런타임 오류 없음.
- 확장 ZIP 10개 파일 및 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- 화면 배치만 변경하며 AI 처리, 데이터 스키마, 네트워크 권한, 앱 버전 변경 없음.
- 설치된 확장은 갱신된 `dist`로 다시 로드해야 적용된다.

## 21:46 - 나의 루틴 일정 생성 알림 카드 추가

### 변경 요약

- 안내일이 된 활성 루틴을 모든 화면 오른쪽 아래의 알림 카드로 표시한다. 제목, 예정 날짜·시간, 프로젝트, 일정 종류와 짧은 메모를 제공한다.
- 카드에서 이번 회차의 일정을 바로 만들거나, 기존 일정 입력 모달에서 수정 후 생성할 수 있다. 알림 전용 루틴은 확인 처리하며 일정을 만들지 않는다.
- 내일 다시 알림, 알림 접기·펼치기, 여러 루틴 순서 이동과 전체 보기 동작을 추가했다. 메모 원문과 기존 회차별 중복 생성 방지 처리를 유지한다.
- 백업 알림과 루틴 카드를 같은 영역에 배치하고 함께 표시할 때 백업은 작은 카드로 제공한다. 작은 화면의 스크롤, 하단 메뉴 간격과 키보드 탐색을 반영했다.
- 확장 프로그램, 단일 HTML 및 ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/components/RoutineReminder.tsx`, `src/components/AppReminderStack.tsx`, `src/components/AppReminderStack.css`
- `src/components/AppShell.tsx`, `src/pages/RoutinesPage.tsx`
- `scripts/test-routine-notifications.mjs`, `scripts/test-routines-ui.mjs`, `package.json`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`, `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`, `npm run lint`, `npm test`, `npm run test:routines`, `git diff --check` 통과.
- 독립 Chromium에서 화면 이동 중 표시·접기 유지, 요약 정보, 직접 생성, 수정 취소·저장, 알림 전용 확인, 내일 다시 알림과 새로고침 후 유지, 날짜 변경 시 재표시를 확인했다. 예정·중지 루틴은 카드 대상에서 제외되며 저장된 일정과 처리 이력을 검증했다.
- 기존 루틴 목록과 확장 알람 예약·탭 재사용·중복 알림 억제·미루기·재시작 동기화 테스트 통과.
- PC 1440px 및 모바일 390px·320px에서 가로 넘침과 백업 카드 겹침이 없음을 확인했다. PC·320px·카드 캡처 육안 검토 및 순서 이동 버튼의 키보드 포커스 확인. 외부 요청을 차단한 테스트에서 브라우저 런타임 오류 없음.
- 확장 ZIP 10개 파일 및 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- 데이터 스키마, 외부 통신, 네트워크 권한과 앱 버전 변경 없음. 일정 생성은 사용자가 버튼을 누를 때만 실행한다.
- 앱 내 카드는 기존 대시보드 안내와 같이 창 표시 설정과 별도로 제공된다. 확장의 안내일 오전 9시 창 활성화 조건과 동작은 유지한다. HTML 버전은 열려 있는 동안 카드를 표시한다.
- 접기는 현재 앱 실행 중 유지되며 새 안내 대상이 생기면 펼쳐진다. 내일 다시 알림은 기존 루틴 처리 이력에 저장한다.
- 설치된 확장은 갱신된 `dist`로 다시 로드해야 적용된다.

## 22:59 - 루틴 카드 세로 압축 및 버튼 가시성 개선

### 변경 요약

- 제목과 반복 주기를 같은 행에 배치하고, 예정 날짜의 라벨과 값을 한 줄로 정리했다. 카드 안쪽 여백과 메모·이력·버튼 사이 간격을 줄였다.
- 수정·일시 중지·삭제 버튼에 배경과 테두리를 적용하고, 삭제 아이콘에 글자를 추가했다. 수정은 파란색, 삭제는 붉은색으로 구분하며 버튼 높이는 기존 32px에서 36px로 늘렸다.
- 건너뛰기 버튼에도 테두리를 적용했다. 기존 프로젝트 색상과 카드 기능을 유지하고 확장·HTML·ZIP 배포 파일을 갱신했다.

### 주요 영향 파일

- `src/components/RoutineList.tsx`, `src/pages/RoutinesPage.css`
- `dist/assets/app.js`, `dist/assets/app.css`, `dist.zip`
- `dist-web/planai.html`, `dist-web/planai.zip`

### 검증

- `npm run build`, `npm run build:single-html`, `npm run lint`, `npm run test:routines`, `git diff --check` 통과.
- 동일한 예정 루틴·처리 이력 샘플의 카드 높이는 PC 304px→219px(약 28% 감소), 모바일 298px→217px로 줄었다.
- 독립 Chromium의 1440px·390px·320px에서 가로 넘침과 런타임 오류 없음. PC 카드와 320px 화면을 육안 검토했다.
- 관리 버튼의 배경·테두리 및 최소 높이 36px, 키보드 처리 이력 펼치기, 긴 제목 수정·저장을 확인했다. 초기 시각 검증 샘플의 존재하지 않는 일정 종류 ID는 실제 기본값으로 수정한 뒤 저장 검사를 재실행해 통과했다.
- 확장 ZIP 10개 파일, 단일 HTML ZIP 1개 파일의 원본 해시 일치 확인.

### 주의사항

- 화면 구성만 변경하며 루틴 처리 규칙, 데이터 스키마, 네트워크 권한과 앱 버전 변경 없음.
- 대시보드에서 공유하는 루틴 목록 카드에도 적용된다. 설치된 확장은 갱신된 `dist`를 다시 로드해야 적용된다.
