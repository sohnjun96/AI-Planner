## 17:43 - 점심 메이트 AI 요청 즉시 취소 방지

### 변경 요약

- 나의 기록 화면의 점심 메이트 AI 정리가 초기 데이터·캐시 갱신 직후 `canceled`로 끝나던 문제를 수정했다.
- 캐시 준비 여부를 구분하고 300ms 안정화 후 요청하며, 날짜·후보 데이터·AI 설정 등 실제 요청 조건이 바뀔 때만 기존 요청을 취소하고 다시 시작하도록 조정했다.
- 분석 실패 상태의 로컬 저장까지 실패하는 경우를 별도로 처리해 처리되지 않은 비동기 오류가 남지 않도록 했다.

### 주요 영향 파일

- `src/pages/ArchivePage.tsx`
- `dist/assets/app.js`
- `docs/updates/2026-08-05.md`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm audit --audit-level=low` (취약점 0건)
- `git diff --check`

### 주의사항

- API 키는 새 저장소나 로그에 복제하지 않으며 기존 메모리 전달 및 Chrome 저장 정책을 유지한다.
- 화면 이탈, 분석 대상 변경 또는 AI 연결 설정 변경 시 진행 중 요청을 취소하는 기존 자원 정리 동작은 유지한다.

## 21:40 - 일요일 기본 주간 및 AI 모델 목록 선택

### 변경 요약

- 새 설정과 주 시작 값이 없는 가져오기 데이터의 기본 요일을 일요일로 변경하고, 주간 통계도 선택한 시작 요일을 따르도록 했다. 사용자가 저장한 월요일 선택은 유지한다.
- AI 설정에서 승인된 MOIP 호스트의 OpenAI 호환 `GET /v1/models`를 사용해 모델 목록을 불러오고 선택할 수 있도록 했다.
- 목록 조회가 실패하거나 결과가 없더라도 모델 식별자를 직접 입력할 수 있으며, 입력값은 저장 전에 검증한다.

### 주요 영향 파일

- `src/constants.ts`
- `src/context/AppDataContext.tsx`
- `src/utils/importBackup.ts`
- `src/agent/llmClient.ts`
- `src/pages/SettingsPage.tsx`
- `dist/assets/app.js`
- `docs/updates/2026-08-05.md`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm audit --audit-level=low` (취약점 0건)
- `git diff --check`

### 주의사항

- 모델 조회 요청은 승인된 HTTPS 호스트로만 보내고 API 키를 응답·오류·변경 이력에 기록하지 않는다.
- 모델 목록은 15초 타임아웃, 256KB 응답 한도, 최대 500개 제한을 적용하며 화면 이탈이나 인증 설정 변경 시 요청을 취소한다.
- 기존 사용자가 명시적으로 저장한 월요일 설정은 데이터 무결성을 위해 자동 변경하지 않는다.

## 21:43 - 노트 자동 추천 기본값 비활성화

### 변경 요약

- 관련 일정 자동 추천과 관련 노트 자동 추천의 신규·누락 설정 기본값을 모두 사용 안 함으로 변경했다.
- 백업 데이터에 해당 설정이 없을 때도 자동 추천이 임의로 활성화되지 않도록 가져오기 기본값을 맞췄다.
- 사용자가 직접 활성화해 저장한 기존 값은 유지한다.

### 주요 영향 파일

- `src/constants.ts`
- `src/pages/SettingsPage.tsx`
- `src/utils/importBackup.ts`
- `dist/assets/app.js`
- `docs/updates/2026-08-05.md`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm audit --audit-level=low` (취약점 0건)
- `git diff --check`

### 주의사항

- 추천 기능 자체는 삭제하지 않으며 사용자가 설정에서 명시적으로 켤 수 있다.
- 기존에 저장된 활성화 선택을 강제로 덮어쓰지 않아 사용자 설정 무결성을 유지한다.

## 21:45 - 대시보드 상단 버튼 높이 통일

### 변경 요약

- 대시보드 상단의 AI 브리핑, 보기 전환, 일정 추가 제어 높이를 52px로 통일했다.
- 대시보드 상단에만 범위를 제한해 다른 화면의 공통 버튼과 보기 전환 컴포넌트 크기는 변경하지 않았다.

### 주요 영향 파일

- `src/index.css`
- `dist/assets/app.css`
- `docs/updates/2026-08-05.md`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `git diff --check`

### 주의사항

- 좁은 화면에서 버튼이 줄바꿈되더라도 각 제어의 높이는 동일하게 유지된다.
- 기능, 외부 통신, 저장 데이터 형식에는 변경이 없다.

## 21:59 - 내부망·외부망 AI 빌드 프로필 분리

### 변경 요약

- 내부망은 기존 MOIP Chat Completions 및 Models 주소를, 외부망 테스트는 OpenAI의 해당 주소를 사용하는 빌드 프로필을 추가했다.
- `npm run build:internal`은 `dist`, `npm run build:external`은 `dist-external`에 서로 분리된 확장 프로그램을 생성하도록 했다.
- 설정 화면에 현재 빌드 프로필을 표시하고, 망별 설정·빌드·Chrome 로드 절차를 문서화했다.

### 주요 영향 파일

- `config/build-profiles.json`
- `scripts/build-profile-config.mjs`
- `scripts/build-extension.mjs`
- `public/manifest.json`
- `src/config/buildProfile.ts`
- `src/agent/llmClient.ts`
- `src/constants.ts`
- `src/pages/SettingsPage.tsx`
- `dist/manifest.json`
- `dist/assets/app.js`
- `docs/BUILD_PROFILES.md`
- `docs/updates/2026-08-05.md`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm run build:external`
- `npm run build:internal`
- `npm audit --audit-level=low` (취약점 0건)
- `git diff --check`

### 주의사항

- 프로필 설정에는 Endpoint만 두며 API 키, 토큰, 비밀번호는 기록하지 않는다.
- 각 산출물은 선택한 HTTPS 출처의 Chrome 호스트 권한과 CSP만 포함하고 반대편 망 주소가 번들에 포함되면 빌드를 실패시킨다.
- 외부망 테스트본과 내부망 운영본은 별도 폴더에서 Chrome에 로드해 저장소와 API 키가 섞이지 않도록 한다.
