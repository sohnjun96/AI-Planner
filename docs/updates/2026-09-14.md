## 13:09 - 루틴 및 일정 알림 개선 커밋 준비

### 변경 요약

- 9월 11~12일 구현한 루틴 관리와 일정 알림 개선의 코드·배포 파일·변경 이력을 하나의 커밋으로 정리한다.
- 공통 일정 입력 검증 파일 끝의 불필요한 빈 줄을 제거했다. 동작 변경은 없다.

### 주요 영향 파일

- `src/utils/taskInput.ts`
- 루틴 및 일정 알림 변경 파일은 `2026-09-11.md`, `2026-09-12.md`에 기록했다.

### 검증

- `npm run typecheck`, `npm run lint`, `npm test` 통과(9개 테스트 묶음).
- 테스트는 샌드박스의 디렉터리 접근 제한으로 한 차례 실패한 뒤 승인된 일반 실행 환경에서 통과했다.

### 주의사항

- 버전은 1.3.0을 유지한다. 질문 풀페이지와 오케스트레이션은 아직 기획 단계로 이번 구현에 포함하지 않는다.
- 관련 없는 로컬 설치 안내 자료와 화면 캡처는 커밋에 포함하지 않는다. 기존 보안·호환성 주의사항은 앞선 변경 이력을 따른다.
