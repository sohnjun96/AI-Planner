## 23:27 - 일정 모달 바깥 클릭 판정 개선

### 변경 요약

- 일정 추가·수정 모달 안에서 시작한 드래그를 바깥에서 끝내도 모달이 닫히지 않도록 수정했다.
- 주 포인터의 왼쪽 버튼 누르기와 놓기가 모두 백드롭에서 발생한 경우에만 모달을 닫도록 판정을 강화했다.

### 주요 영향 파일

- `src/components/TaskModal.tsx`
- `dist/assets/app.js`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 브라우저에서 일정 수정 모달 내부에서 바깥으로 드래그한 뒤 모달이 유지되고, 백드롭 클릭 시에는 닫히는지 확인

### 주의사항

- 새 외부 패키지나 권한 변경은 없으며, 일정 추가·수정 공용 모달에 동일하게 적용된다.

## 23:30 - 애플리케이션 버전 1.1.2 반영

### 변경 요약

- 애플리케이션과 Chrome 확장 프로그램의 기준 버전을 `1.1.2`로 올렸다.
- 내부 배포본의 표시 버전은 `1.1.2`, 외부 테스트 배포본은 `1.1.2-external`로 생성했다.

### 주요 영향 파일

- `package.json`
- `package-lock.json`
- `public/manifest.json`
- `dist/manifest.json`

### 검증

- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 두 빌드의 `manifest.json` 버전 및 표시 버전 확인

### 주의사항

- 의존성 패키지의 버전은 변경하지 않았으며, 기존 권한과 엔드포인트 설정을 유지했다.

## 23:36 - 모든 모달의 바깥 클릭 판정 통일

### 변경 요약

- 모달 내부에서 시작한 드래그를 바깥에서 끝내도 모달이 닫히지 않도록 공통 백드롭 컴포넌트를 추가했다.
- AI 일정 추가, 질문, 노트 추가·분류·이력·액션, 프로젝트 설정, AI 세부 설정, 모델 선택, 백업, 브리핑, 도움말, 일정 알림을 포함한 백드롭 모달 15곳에 동일한 판정을 적용했다.
- 주 포인터의 왼쪽 버튼 누르기와 놓기가 모두 백드롭에서 발생한 경우에만 닫기 요청을 실행한다.

### 주요 영향 파일

- `src/components/ModalBackdrop.tsx`
- `src/components/AppShell.tsx`
- `src/components/AskDataModal.tsx`
- `src/components/DailyBriefing.tsx`
- `src/components/HelpModal.tsx`
- `src/components/NoteActionModal.tsx`
- `src/components/NoteMetaModal.tsx`
- `src/components/NoteQuickAddModal.tsx`
- `src/components/ScheduleReminderModal.tsx`
- `src/components/TaskModal.tsx`
- `src/pages/NotesPage.tsx`
- `src/pages/ProjectsPage.tsx`
- `src/pages/SettingsPage.tsx`
- `dist/assets/app.js`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 직접 백드롭 `onClick` 잔존 여부 0건 및 공통 백드롭 적용 15곳 확인
- 브라우저에서 AI 일정 추가, 노트 추가, 질문, AI 맞춤 규칙, 프로젝트 설정 모달의 내부→바깥 드래그 유지와 바깥 클릭 닫힘 확인

### 주의사항

- 새 외부 패키지나 권한 변경은 없으며, 각 모달의 닫기 전 저장 확인·처리 중 차단 로직은 그대로 유지된다.

## 23:43 - 앱 내부 백업 저장 검증 오류 수정

### 변경 요약

- Chrome 저장소가 객체 속성 순서를 다르게 반환해도 실제 백업 내용이 같으면 저장 검증을 통과하도록 수정했다.
- 저장 후 백업 항목을 다시 정규화하고 ID, 생성 시각, 사유, 백업 본문을 각각 비교해 누락·변조 여부는 계속 엄격하게 검증한다.
- 속성 순서 변경은 허용하되 본문 변경이나 항목 누락은 거부하는 자동화 테스트를 추가했다.

### 주요 영향 파일

- `src/context/AppDataContext.tsx`
- `src/utils/autoBackupIntegrity.ts`
- `tests/autoBackupIntegrity.test.ts`
- `scripts/run-tests.mjs`
- `dist/assets/app.js`

### 검증

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build:internal`
- `npm run build:external`
- 설정 화면에서 앱 내부 백업 생성 성공, 목록 1건 반영 및 검증용 백업 삭제 확인

### 주의사항

- 백업 데이터의 실제 필드와 본문은 완전 일치해야 하며, 저장 한도·민감정보 제거·입력 정규화 정책은 유지된다.
